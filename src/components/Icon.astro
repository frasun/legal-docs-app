---
import { Icon as AstroIcon } from "astro-icon";

export enum IconSize {
	sm = 20,
	base = 24,
	xl = 32,
}

const { size = IconSize.base } = Astro.props;
---

<AstroIcon width={size} height={size} {...Astro.props} />
