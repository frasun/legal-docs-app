---
bills: {
    landlord: ['zużycie energii', 'zużycie gazu', 'zużycie wody', 'wywóz śmieci'],
    tenant: ['dostęp do internetu', 'usługa telewizyjna']
}
---
Wynajmujący: {frontmatter.bills.landlord.length ? frontmatter.bills.landlord.join(', ') : 'brak'}

Najemca: {frontmatter.bills.tenant.length ? frontmatter.bills.tenant.join(', ') : 'brak'}