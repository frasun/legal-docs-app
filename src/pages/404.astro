---
import EmptyScreen from "@components/EmptyScreen.astro";
import Layout, { LayoutType } from "../layouts/App.astro";
import getTitle from "@utils/pageTitle";
---

<Layout
  pageName={getTitle("Podana strona nie istnieje (404)")}
  title=""
  layout={LayoutType.centered}
>
  <EmptyScreen cta="Idź do strony głównej" ctaUrl={`${Astro.url.origin}`}>
    Przykro nam, podana strona nie istnieje (404)
  </EmptyScreen>
</Layout>
