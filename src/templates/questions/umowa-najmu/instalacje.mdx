---
systems: [$system]
question: Jakie instalacje dostępne są w lokalu?
systemslib: [elektryczna, gazowa, wodna, centralne ogrzewanie, kanalizacja, klimatyzacja]
---
Zaznacz wszystkie pasujące odpowiedzi

{frontmatter.systemslib.map(system => (
    <>
    <input id={system} type="checkbox" checked={frontmatter.systems.includes(system)} />
    <label for={system}>{system}</label>
    <br />
    </>
))}
