---
question: Do jakich celów przeznaczony będzie lokal?
purpose: "do celów mieszkalnych"
---
import Checkboxes from '@components/Checkboxes.astro'
import {purposeEnum} from '@content/documents/umowa-najmu-lokalu/_schema'

<fieldset>
    <legend>Zaznacz wszystkie pasujące odpowiedzi</legend>
    <Checkboxes values={purposeEnum} selected={frontmatter.purpose} token="purpose" required size="lg" />
</fieldset>