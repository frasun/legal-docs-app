---
import { UUID } from "mongodb";
import Icon, { IconSize } from "@components/Icon.astro";

interface Props {
  id: UUID;
}

const { id } = Astro.props;
---

<delete-document data-document-id={id} title="Usuń szkic">
  <Icon name="delete" size={IconSize.sm} />
</delete-document>

<style>
  delete-document {
    @apply btn btn-alt btn-icon;
  }
</style>

<script>
  import "@wc/DeleteDocument";
</script>
