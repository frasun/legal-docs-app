---
title: Umowa najmu lokalu
address: $address
rooms: [$room]
systems: [$system]
equipment: [$equipment]
purpose: $purpose
rent: { value: $rent.value, method: $rent.method, due: $rent.due }
bills:
  {
    landlord: [$bills.landlord],
    tenant: [$bills.tenant],
  }
deposit: { value: $deposit.value, method: $deposit.method }
repairs: $repairs
termination: $termination
return: $return
---

# Umowa najmu lokalu

1. Przedmiot Umowy

   1. Umowa dotyczy lokalu o powierzchni 65,23 m<sup>2</sup>, znajdującego się pod adresem: **{frontmatter.address}**.
   2. W skład lokalu wchodzą następujące pomieszczenia:
      <ul>
        {(function () {
          return frontmatter.rooms.map((item) => <li>{item}</li>);
        })()}
      </ul>
   3. Lokal posiada instalacje:
      <ul>
        {(function () {
          return frontmatter.systems.map((item) => <li>{item}</li>);
        })()}
      </ul>
   4. Na stanie lokalu znajduje się wyposażenie:
      <ul>
        {(function () {
          return frontmatter.equipment.map((item) => <li>{item}</li>);
        })()}
      </ul>

2. Przebieg umowy

   1. Lokal będzie użytkowany tylko do {frontmatter.purpose}.
   2. Czynsz w wysokości {frontmatter.rent.value} zł, {frontmatter.rent.method} do {frontmatter.rent.due}. dnia miesiąca.
   3. Opłaty za media będą ponosić
      - Wynajmujący
        <ul>
        {(function () {
          return frontmatter.bills.landlord.map((item) => <li>{item}</li>);
        })()}
        </ul>
      - Najemca
        <ul>
        {(function () {
          return frontmatter.bills.tenant.map((item) => <li>{item}</li>);
        })()}
        </ul>
   4. Kaucja w wysokości {frontmatter.deposit.value} zł, {frontmatter.deposit.method}.
   5. Najemca pokrywa koszty napraw do {frontmatter.repairs} zł.
   6. Każda ze Stron może wypowiedzieć umowę z okresem wypowiedzenia {frontmatter.termination} dni.
   7. Zwrot lokalu powinien nastąpic do {frontmatter.return}. dnia od daty zakończenia Umowy.

3. Prawa i obowiązki

4. Czas trwania umowy

5. Oświadczenia
