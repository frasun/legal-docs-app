---
question: Do jakich celów przeznaczony będzie lokal?
purpose: ["do celów mieszkalnych"]
---
import Checkboxes from '@components/Checkboxes.astro'
import {purposeEnum} from '@schema/umowa-najmu-lokalu'

<Checkboxes values={purposeEnum} selected={frontmatter.purpose} token="purpose" size="lg" label="Zaznacz przynajmniej jedną odpowiedź" />