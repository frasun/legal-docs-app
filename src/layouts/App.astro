---
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---

<Layout title={`Prawniczek - ${frontmatter.title}`}>
  <article class="prose">
    <slot />
  </article>
</Layout>

<style>
  article {
    @apply container bg-white p-30 lg:p-50 my-30 w-[90vw];
  }
</style>
