---
slug: kaucja
deposit: 2500
depositmethod: przelewem na konto bankowe
depositmethodlib: [przelewem na konto bankowe, gotówką]
question: Ile będzie wynosić kaucja i jakie są warunki jej zwrotu?
---
wysokość <input type="text" name="deposit" value={frontmatter.deposit} required step=".01" min="1" />

płatność 
<select name="depositmethod" required>
{frontmatter.depositmethodlib.map(m => <option value={m} selected={frontmatter.depositmethod.includes(m)}>{m}</option>)}
</select>