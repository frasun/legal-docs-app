---
import Icon, { IconSize } from "@components/Icon.astro";

interface Props {
  id: string;
}

const { id } = Astro.props;
---

<copy-document data-document-id={id} title="Utwórz kopię">
  <Icon name="copy-document" size={IconSize.sm} />
</copy-document>

<style>
  copy-document {
    @apply btn btn-alt btn-icon;
  }
</style>

<script>
  import "@wc/CopyDocument";
</script>
